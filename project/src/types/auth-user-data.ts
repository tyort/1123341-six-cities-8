/* eslint-disable camelcase */
export type AuthUserData = {
  email: string;
  password: string;
};

export type AuthInfo = {
  avatar_url: string,
  email: string,
  id: number,
  is_pro: boolean,
  name: string,
  token: string,
}
